const config = {
  clientIdPaypal: String(process.env.CLIENT_ID_PP),
  googleMapApiKey: String(process.env.GC_API_KEY),
  positionStackApiKey: String(process.env.PS_API_KEY),
};

export default config;
